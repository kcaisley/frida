{
  "configurations": [
    {
      "type": "debugpy",
      "request": "launch",
      "name": "Launch SAR ADC Simulation",
      "cwd": "${workspaceFolder}/sim",
      "program": "${workspaceFolder}/sim/sar_adc_sim.py",
      "console": "integratedTerminal"
    }
  ]
}